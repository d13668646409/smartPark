import * as actionTypes from './actionTypes';

export const  loadStart = (payload) =>({
    type: actionTypes.LOAD_START,
    payload
});

export const  loadEnd = (payload) =>({
    type: actionTypes.LOAD_START,
    payload
});